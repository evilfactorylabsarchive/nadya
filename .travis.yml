language: node_js
node_js:
  - "node"
cache: yarn
branches:
  only:
    - canary
    - master

before_script:
  - yarn global add now

install:
  - yarn global add codecov

script:
  - yarn workspace app test --coverage && codecov -p app/

after_script:
  - bash ./deploy.sh
